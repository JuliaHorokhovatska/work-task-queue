import { CalculateProgressWidthPipe } from './calculate-progress-width-pipe';

describe('CalculateProgressWidthPipe', () => {
  it('create an instance', () => {
    const pipe = new CalculateProgressWidthPipe();
    expect(pipe).toBeTruthy();
  });
});
