import { Intersection } from './intersection';

describe('Intersection', () => {
  it('should create an instance', () => {
    const directive = new Intersection();
    expect(directive).toBeTruthy();
  });
});
